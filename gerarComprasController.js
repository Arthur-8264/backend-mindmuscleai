const gerarComprasController = async (req, res) => {
  // Aqui vai a lógica para usar a API do Gemini com base na localização
  res.json({ mensagem: 'Função de gerar compras ainda não implementada' });
};

module.exports = gerarComprasController;
